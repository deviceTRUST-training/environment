# Delete Connection Groups
DELETE FROM guacamole_connection_group;

{% set count = my_super_count_value|int %}

{% for n in range(count) %}
    SELECT * FROM guacamole_entity WHERE name = '{{ guac_root_connection_group_prefix }}{{ n + 1 }}'
{% endfor %}